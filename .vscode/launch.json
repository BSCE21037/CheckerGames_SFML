{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug checkers (lldb)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/checkers",
            "args": [],
            "cwd": "${workspaceFolder}",
            "MIMode": "lldb",
            "preLaunchTask": "build checkers"
        }
    ]
}
